import { FC } from 'react'

export const metadata = {
    title: 'Business - Settings',
    description: 'Administrative solutions / Settings',
}


const page: FC = ({ }) => {
    return <div>Settings</div>
}

export default page